# Contexte de build Heroku (racine) : l'image combine le front Vue ET le
# backend Rust. On garde donc les sources du front (src/, public/, index.html…)
# et le dossier backend/, mais on exclut les artefacts et le superflu.
**/target/
node_modules/
dist/
.git/
ios/
*.db
*.db-shm
*.db-wal
.env
.env.local
